#include "ll.h"
int main(){
    struct node *start=NULL;
    return 0;
}
struct node *start=NULL;
    insbeg(&start,2);
    insbeg(&start,3);
    insbeg(&start,4);
    insbeg(&start,5);
    insbeg(&start,6);
    insbeg(&start,7);
    traverse(&start);
    return 0;